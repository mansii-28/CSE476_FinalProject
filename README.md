# CSE476_FinalProject

Clone the repo and run 
    python3 -m venv venv
    source venv/bin/activate
    pip install -r requirements.txt
To leave venv, run "deactivate"

pytests is used for individual test files to run specific techniques on their own.
Usage example: pytest unittests/test_cot.py -v